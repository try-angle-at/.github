# 팀 작업 운영 안내

조직 Project에서 전체 우선순위와 진행 상황을 보고, 각 저장소의 Issue에서 요청·합의·검증을 기록하고, PR에서 실제 변경을 리뷰합니다.

## 작업을 시작하는 방법

1. 변경을 주로 책임지는 저장소에서 **New issue**를 엽니다.
2. 여러 저장소에 영향을 주면 **CR · 팀 간 변경 요청**, 개별 구현이면 **Task**, 오류면 **Bug** 양식을 선택합니다.
3. [조직 통합 Project](https://github.com/orgs/try-angle-at/projects/2)에 이슈를 연결합니다. 공통 양식으로 작성하고 보드 쓰기 권한이 있으면 자동으로 연결됩니다. 빈 이슈·API로 생성한 이슈나 보드 쓰기 권한이 없는 작성자의 이슈는 직접 추가합니다. 담당자는 실제 책임질 사람 한 명을 합의해서 지정합니다.
4. 요청을 검토하고 우선순위를 정한 뒤 `Ready`로 옮깁니다. 요청 등록만으로 일정·담당자 합의가 완료되지는 않습니다.
5. 구현 PR을 이슈에 연결하고, 완료 조건에 필요한 확인 결과를 남깁니다.

## Project 상태

| Status | 의미 |
|---|---|
| Backlog | 접수한 요청. 범위·우선순위·담당자 검토 전 |
| Ready | 범위·담당자·완료 조건을 합의했고 착수 가능 |
| In Progress | 작업 중. CR은 관련 레포 반영이 진행 중인 상태 |
| In Review | 코드 리뷰 또는 최종 확인 중 |
| Done | 해당 이슈의 완료 조건 충족 |

`Status`, 담당자, 확정한 `Priority`는 Project와 이슈의 메타데이터로 관리합니다. 같은 상태를 별도 문서에 다시 적지 않습니다. 막힌 작업에는 `blocked` 라벨을 붙이고 기다리는 이유와 선행 이슈를 댓글로 남깁니다. 해결되면 라벨을 제거합니다.

우선순위는 `P0`(장애·데이터 유실·핵심 기능 차단), `P1`(주요 기능·품질 개선), `P2`(일반 개선·정리)를 사용합니다. CR 양식의 요청 우선순위는 제안이며, 합의 후 Project의 `Priority`를 기준으로 운영합니다.

## 여러 저장소에 걸친 변경

- CR은 변경 계약과 전체 완료 조건을 담습니다. 필요한 구현 작업을 각 저장소에 만들고 CR의 하위 이슈로 연결합니다.
- CR보다 큰 목표를 묶는 추적 이슈에는 `tracking` 라벨을 사용합니다.
- 새 이슈는 `owner/repo#번호`로 식별합니다. 조직 전체 CR 일련번호를 따로 발급할 필요는 없습니다. 기존 CR 이름은 유지해도 됩니다.
- 모든 저장소에 같은 요청을 복제하지 않습니다. 독립적으로 맡고 검증할 작업이 있을 때 구현 이슈를 만듭니다.
- 기존 이슈가 다른 저장소에 있다면 그대로 참조하고, 새 구현 작업부터 책임 저장소에 둡니다.
- 이슈 본문에는 현재 합의한 규칙을, 댓글에는 결정 과정과 검증 근거를 남깁니다. 코드 저장소의 스펙은 확정한 계약과 버전 이력을 보관합니다.

CR의 `CR Stage`는 `제안 → 합의 → 반영 중 → 검증 중 → 검증 완료`이며, 진행하지 않기로 결정하면 `철회`로 기록합니다. 철회는 검증 완료와 구별하고 이슈를 **Close as not planned**로 닫습니다. 철회된 CR의 `Status`는 종료를 뜻하는 `Done`으로 둘 수 있지만, 성공 여부는 `CR Stage`와 이슈 종료 사유로 판단합니다.

서버·클라이언트·소비 화면의 반영 여부와 배포 근거는 CR 본문의 체크리스트에 남깁니다. 배포 순서는 변경별로 합의합니다. 서버 구현이나 PR 병합만 끝난 CR은 계속 열어 두고, 요청한 전체 흐름을 검증한 뒤 `CR Stage = 검증 완료`, `Status = Done`으로 바꾸고 닫습니다.

## PR과 완료 기준

- 구현 이슈의 완료 조건이 기본 브랜치 병합으로 모두 충족되면 PR 본문에 `Closes #번호`를 사용합니다. 다른 저장소는 `Closes owner/repo#번호`로 적습니다.
- 배포·실기기 확인 등 남은 조건이 있으면 `Refs owner/repo#번호`로 연결하고, 검증 후 담당자가 이슈를 닫습니다.
- 하위 구현 PR에서는 상위 CR을 `Refs`로 연결합니다. 상위 CR의 통합 검증이 끝나기 전에는 자동 종료 키워드를 사용하지 않습니다.
- 검증 댓글에는 검증자·날짜·환경·빌드·결과와 근거 링크를 남깁니다.

## 짧은 정기 점검

주 1회 `Ready`, 진행 중인 작업, `blocked` 작업을 함께 확인합니다. 담당자가 없는 요청과 검증이 남은 CR을 우선 확인하고 다음 착수 작업을 정합니다.

## 공통 양식 적용 범위

이 저장소의 `.github/ISSUE_TEMPLATE`와 PR 템플릿은 별도 양식이 없는 조직 저장소에 기본으로 제공됩니다. 저장소 자체에 이슈 양식이나 `config.yml`이 있으면 그 저장소의 설정이 우선합니다.

이 공개 저장소에는 일반 양식과 운영 규칙을 보관합니다. 실제 변경 내용, 내부 스펙, 로그와 검증 근거는 해당 작업 저장소에 기록합니다.

참고: [조직 기본 파일](https://docs.github.com/en/communities/setting-up-your-project-for-healthy-contributions/creating-a-default-community-health-file), [이슈와 PR 연결](https://docs.github.com/en/issues/tracking-your-work-with-issues/using-issues/linking-a-pull-request-to-an-issue).
